<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="QuinielaComunitaria.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:QuinielaComunitaria.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="QuinielaComunitaria">

    <ShellContent Title="Usuarios" ContentTemplate="{DataTemplate views:MainPage}" 
                  Route="MainPage" />
    <ShellContent Title="Quiniela" ContentTemplate="{DataTemplate views:QuinielaPage}"
                  Route="Quiniela" />

</Shell>
